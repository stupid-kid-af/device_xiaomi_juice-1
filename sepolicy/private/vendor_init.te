#allow vendor_init persist_file:lnk_file read;

# Allow vendor_init to set vendor_camera_prop
#set_prop(vendor_init, vendor_camera_prop)

#allow vendor_init vendor_camera_prop:file {read getattr map open};
